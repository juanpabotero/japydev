---
import { getCollection } from 'astro:content';
import Note from '../../components/Note.astro';
import Layout from '../../layouts/Layout.astro';

const notes = await getCollection('notes');
---

<Layout title="JapyDev | Notas">
  <p>
    <span class="text-amber-300">Notas y recursos</span> tomados a lo largo de mi
    aprendizaje en el
    <b>desarrollo web</b>.
  </p>
  <p class="pb-10">
    Anotaciones de distintas tecnologías, herramientas, lenguajes, frameworks,
    recursos y más.
  </p>
  <section class="sm:columns-2 gap-x-4">
    {
      notes.map((note) => (
        <Note
          id={note.data.id}
          img={note.data.heroImage}
          link={`/notes/${note.slug}/`}
          title={note.data.title}
        />
      ))
    }
  </section>
</Layout>
