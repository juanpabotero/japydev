---
import type { CollectionEntry } from 'astro:content';
import Layout from './Layout.astro';

type Props = CollectionEntry<'notes'>['data'];

const { title } = Astro.props;
---

<Layout title={`JapyDev | ${title}`}>
  <article class="post">
    <slot />
  </article>
</Layout>
